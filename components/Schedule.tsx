import styles from "@/styles/schedule.module.css";

export default function Schedule() {
  return (
    <section className={`container ${styles.schedule_Main}`}>
      <p>Schedule</p>
      <div>
        <p>Fri</p>
        <p>Sep 20</p>
      </div>
    </section>
  );
}
