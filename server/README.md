#### Server Setup

Install all the dependencies

```
npm install
```

Create `.env` file and provide these informations mentioned in `.env.example`

```
PORT=
MONGODB_URI=
SALT=
JWT_SECRET=
```

To run server

```
nodemon server.js
```
